[project]
name = "app"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = "~=3.12.0"
dependencies = [
  "beautifulsoup4==4.14.3",
  "crawlee[parsel,httpx]==1.6.0",
  "fastapi[standard]==0.115.0",
  "mcp[cli]==1.9.0"
]

[dependency-groups]
dev = [
  "httpx~=0.28.0",
  "pytest~=8.4.0",
  "ruff~=0.12.0",
]

[tool.pytest]
addopts = ["-s"]

[tool.uv]
add-bounds = "exact"
